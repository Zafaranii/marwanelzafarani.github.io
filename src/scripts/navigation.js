/**
 * Navigation Module
 * Updated for multi-page navigation structure
 */

const Navigation = {
  init() {
    // No need for complex hash-based navigation since we're using separate pages
    // The active state of navigation links is set directly in each page's HTML
    
    // We could add smooth transitions or other enhancements here in the future
    
    // If we need to handle any dynamic navigation features, we can add them here
    
    // For now, we just let the browser handle navigation between pages naturally
  }
};
